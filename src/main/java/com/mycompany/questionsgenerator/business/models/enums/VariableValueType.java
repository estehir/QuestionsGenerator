package com.mycompany.questionsgenerator.business.models.enums;

public enum VariableValueType {
    TEXT,       // texto livre
    NUMBER,     // número livre
    ENUM        // valor pré-definido (lista)
}
